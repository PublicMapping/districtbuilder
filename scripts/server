#!/bin/bash

set -e

function usage() {
    echo -n \
        "Usage: $(basename "$0")
Start application.
"
}

if [ "${BASH_SOURCE[0]}" = "${0}" ]; then
    if [ "${1:-}" = "--help" ]; then
        usage
    else
        ./scripts/use_version
        yarn run start
    fi
fi
