#!/bin/bash

set -e

function usage() {
    echo -n \
        "Usage: $(basename "$0")
Run migrations using typeorm.
"
}

if [ "${BASH_SOURCE[0]}" = "${0}" ]; then
    if [ "${1:-}" = "--help" ]; then
        usage
    else
        docker-compose \
            run --rm server yarn run migration:run
    fi
fi
